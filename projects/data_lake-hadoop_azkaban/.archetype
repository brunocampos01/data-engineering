/docs
/dist
    /az-flows
/connector-${app-name}
    /auth
    /components
    /lib
    client.py
    __init__.py
    /gradle
    build.gradle
    gradlew
    gradlew.bat
    settings.gradle
/udf
    /udf
        /${udf-name}
            /src
                /main/${language}
                /test/${language}
            /lib
            /gradle
            build.gradle
            gradlew
            gradlew.bat
            settings.gradle
    /udtf
        /${udf-name}
            /src
                /main/${language}
                /test/${language}
            /lib
            /gradle
            build.gradle
            gradlew
            gradlew.bat
            settings.gradle
    /udaf
        /${udf-name}
            /src
                /main/${language}
                /test/${language}
            /lib
        /gradle
        build.gradle
        gradlew
        gradlew.bat
        settings.gradle
/elt
    /ingestion
        /sqoop.data.extraction
            /app.${site}.${app-name}
                ${module-name}.update.overwrite.sh
                ${module-name}.update.incremental.sh
                ${module-name}.create.jobs.sh
            /...
        /https.data.extraction
            /app.${site}.${app-name}
                ${module-name}.update.overwrite.sh
                ${module-name}.update.incremental.sh
                ${module-name}.create.jobs.sh
            /...
        /spark.data.extraction
            /app.${site}.${app-name}
                ${module-name}.update.overwrite.py
                ${module-name}.update.incremental.py
                ${module-name}.create.jobs.py
            /...
    /scheduling
        /azkaban.data.flows
            /app.${app-name}
                ${module-name}.data.collect.project
                ${module-name}.data.collect.${table-name}.flow
            /...
    /transform
        /hive.data.sync
        /hive.data.sort
        /hive.data.combine
        /hive.data.clean
        /hive.data.filter
        /hive.data.standardize
        /hive.data.denormalization
        /hive.data.synthesise
        /hive.data.create
        /hplsql.data.reflect
        /pig.data.filter
        /spark.data.merge
    /schema
        /hive.datamart.model
    /gradle
    build.gradle
    gradlew
    gradlew.bat
    settings.gradle
.gitattributes
.gitignore
README.md
